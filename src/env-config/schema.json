{
  "$schema": "http://json-schema.org/schema",
  "$id": "SchematicsEnvConfig",
  "title": "Environment Configuration Schema",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "Tên của project để tạo cấu hình môi trường",
      "$default": {
        "$source": "projectName"
      }
    }
  },
  "required": []
}